# Grid Layout Backup

Snapshot of the card-grid portal layout before the road-themed redesign (Jul 2026).

To restore:
1. Copy `DashboardGrid.tsx` and `AppCard.tsx` back to `src/components/dashboard/`
2. Copy `page.tsx` back to `src/app/page.tsx`
3. Switch `page.tsx` import from `RoadDashboard` to `DashboardGrid`
